rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ Allow all for emulator only
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

